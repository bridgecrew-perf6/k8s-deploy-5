apiVersion: extensions/v1beta1
kind: Ingress
metadata:
 name: static1-ingress
 namespace: static1
spec:
 rules:
 - host: static1.joshna.xyz
   http:
     paths:
     - path:
       backend:
         serviceName: static1-svc
         servicePort: 80
